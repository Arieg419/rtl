import { Component } from "react";
interface State {
}
interface Props {
    imageUri: string;
}
declare class Section extends Component<Props, State> {
    render(): JSX.Element;
}
export default Section;
