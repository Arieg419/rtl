import * as React from "react";
import { View, Text } from "react-native";

interface Props {}

export const Schema: React.FC<Props> = () => {
  return (
    <View>
      <Text>Schema page</Text>
    </View>
  );
};
