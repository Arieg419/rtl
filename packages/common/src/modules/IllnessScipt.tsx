import * as React from "react";
import { View, Text } from "react-native";

interface Props {}

export const IllnessScript: React.FC<Props> = () => {
  return (
    <View>
      <Text>Illness script page</Text>
    </View>
  );
};
